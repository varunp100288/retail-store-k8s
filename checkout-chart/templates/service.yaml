apiVersion: v1
kind: Service
metadata:
  name: checkout
  namespace: retail
spec:
  selector:
    app: checkout
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP